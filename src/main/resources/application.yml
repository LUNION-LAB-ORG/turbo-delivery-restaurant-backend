spring:
  application:
    name: turbo-restaurant
  datasource:
    url: jdbc:postgresql://147.79.101.226:5432/turbo-resto
    username: turbo
    password: turbo
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000         # 5 minutes
      max-lifetime: 1800000        # 30 minutes (doit être < au timeout serveur)
      connection-timeout: 60000    # 60 secondes pour obtenir une connexion
      validation-timeout: 10000    # 10 secondes pour valider la connexion
      keepalive-time: 300000       # ping toutes les 5 minutes
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        formatsql: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

jwt:
  time-exp: 24

password:
  max-tentative: 3
  expired-delay: 30
  length: 8

code-opt:
  length: 4
  delay: 5

doc:
  root: /Users/andersonkouadio/Documents/Projets/TURBO DELIVERY/storage

front:
  base_url: http://localhost:3000

picture:
  max: 5
paginate:
  size: 20

erp:
  server:
    address: http://localhost:8000/api

backend:
  server:
    address: http://localhost:8080/api
